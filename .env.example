APP_ENV=dev
APP_DEBUG=true
APP_TIMEZONE=UTC
# APP_CURRENCY=USD

# Security
ENCRYPTION_KEY=

# Database
DB_HOST=immunity_database
DB_PORT=5432
DB_NAME=immunity
DB_USERNAME=dev
DB_PASSWORD=dev

# Moralis API key for token-price lookups in the indexer. Optional in v1
# (without it, value_at_risk_usd stays NULL). Production: `fly secrets set`.
MORALIS_API_KEY=
