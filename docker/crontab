SHELL=/bin/sh
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

# No scheduled HTTP tasks at the moment. The on-chain indexer (immunity_indexer
# service) drives all live data hydration; cron stays as scaffolding for any
# future scheduled HTTP-call jobs that aren't a fit for the long-running worker.
